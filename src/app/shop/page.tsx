import { AssetPlaceholder } from '@/components/AssetPlaceholder'; import { SiteFrame } from '@/components/SiteFrame'; import { products } from '@/data/products'; import { siteConfig } from '@/config/site';
export default function Shop(){return <SiteFrame><section className="shop-callout page-shop"><div className="shell split"><div><p className="eyebrow">CURATED DISCOVERIES</p><h1>Leufay Link<br/><em>Shoppe</em></h1><p>Discover anime-inspired fashion, merchandise, accessories, fragrance, and other interesting finds curated for the Leufay community.</p></div><AssetPlaceholder label="SHOPPE MASCOT / shopping pose"/></div></section><section className="shell section"><p className="eyebrow">EXPLORE THE SHELVES</p><div className="product-grid">{products.map(product=><article className="product" key={product.id}><AssetPlaceholder label={`SHOP IMAGE / ${product.name}`}/><p className="tag">{product.category}</p><h2>{product.name}</h2><p>{product.description}</p><a className="text-link" href={product.affiliateUrl || siteConfig.shoppeUrl || '#'}>Open curated link <span>↗</span></a></article>)}</div><p className="affiliate-note">Affiliate URLs are configured in <code>src/data/products.ts</code> or <code>src/config/site.ts</code>. No purchase flow is hosted on this website.</p></section></SiteFrame>}
