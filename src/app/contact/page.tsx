import { AssetPlaceholder } from '@/components/AssetPlaceholder'; import { SiteFrame } from '@/components/SiteFrame'; import { siteConfig } from '@/config/site';
export default function Contact(){return <SiteFrame><section className="page-hero shell contact-hero"><p className="eyebrow">OPEN INVITATION</p><h1>Have an<br/><em>Idea?</em></h1><p>Let’s talk about content, collaboration, creative projects, or something completely new.</p><AssetPlaceholder label="MASCOT / contact pose"/></section><section className="shell contact-layout"><form action={siteConfig.email ? `mailto:${siteConfig.email}` : undefined}><p className="eyebrow">SEND A NOTE</p><label>Name<input name="name" required placeholder="Your name"/></label><label>Email<input type="email" name="email" required placeholder="you@example.com"/></label><label>What’s on your mind?<textarea name="message" required placeholder="Tell us a little about it..." rows={6}/></label><button className="button orange" type="submit">Send message →</button><p className="caption">This static form opens your email application once an email address is configured in <code>src/config/site.ts</code>.</p></form><aside><p className="eyebrow">FIND US</p><h2>Drop by the feed.</h2><a className="text-link" href={siteConfig.instagram || '#'}>Instagram ↗</a><a className="text-link" href={siteConfig.email ? `mailto:${siteConfig.email}` : '#'}>Email us ↗</a></aside></section></SiteFrame>}
