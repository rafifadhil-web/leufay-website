import Link from 'next/link'; import { AssetPlaceholder } from '@/components/AssetPlaceholder'; import { SiteFrame } from '@/components/SiteFrame'; import { projects } from '@/data/projects';
export default function Projects(){return <SiteFrame><section className="page-hero shell compact"><p className="eyebrow">IN THE WORKS</p><h1>Projects with<br/><em>personality.</em></h1><p>Different ideas, mediums, and moments from the expanding Leufay universe.</p></section><section className="shell project-rows page-projects">{projects.map((p,i)=><article className={`project-row project-${i}`} key={p.name}><AssetPlaceholder label={`PROJECT COVER / ${p.name}`}/><div><p className="tag">{p.category} · {p.status}</p><h2>{p.name}</h2><p>{p.description}</p><Link className="text-link" href={p.href}>Explore {p.name} <span>→</span></Link></div></article>)}</section></SiteFrame>}
