'use client';

import Link from 'next/link'; import { AssetPlaceholder } from '@/components/AssetPlaceholder'; import { SiteFrame } from '@/components/SiteFrame'; import { useLanguage } from '@/i18n/LanguageProvider'; import { projects } from '@/data/projects';
export default function Projects(){const { t, language } = useLanguage();return <SiteFrame><section className="page-hero shell compact"><p className="eyebrow">{language === "en" ? "IN THE WORKS" : "DALAM PENGERJAAN"}</p><h1>{language === "en" ? "Projects with" : "Proyek dengan"}<br/><em>{language === "en" ? "personality." : "karakter."}</em></h1><p>{language === "en" ? "Different ideas, mediums, and moments from the expanding Leufay universe." : "Beragam ide, media, dan momen dari semesta Leufay yang terus berkembang."}</p></section><section className="shell project-rows page-projects">{projects.map((p,i)=><article className={`project-row project-${i}`} key={p.name.en}><AssetPlaceholder label={`PROJECT COVER / ${p.name[language]}`}/><div><p className="tag">{p.category[language]} · {p.status[language]}</p><h2>{p.name[language]}</h2><p>{p.description[language]}</p><Link className="text-link" href={p.href}>{language === "en" ? "Explore" : "Jelajahi"} {p.name[language]} <span>→</span></Link></div></article>)}</section></SiteFrame>}
